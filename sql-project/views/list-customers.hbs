

<main class="container">
    <div class="row">
        {{#if messages}}
        <div class="alert alert-{{status}}">{{messages}}</div>
        {{/if}}
        {{#if customers}}
        <div class="toolbar">
            <label class="form-label">Filter companies</label>
            <form class="form-control bg-dark d-flex justify-content-center" method="get" action="/list-customers">
                <select class="form-control me-1" name="company_id" id="company_filter">
                    <option value="0">Unemployed</option>
                    {{#each companies}}
                    <option value="{{id}}"{{#if selected}}selected="selected"{{/if}}>{{name}}</option>
                    {{/each}}
                </select>
                <select class="form-control ms-1 me-1" name="customer_col" id="customer_col">
                    <option value="name">Name</option>
                    <option value="surname">Surname</option>
                    <option value="email">Email</option>
                    <option value="company_id">Company</option>
                </select>
                <select class="form-control me-1 ms-1" name="position" id="position">
                    <option value="ASC">a-z</option>
                    <option value="DESC">z-a</option>
                </select>
                <button type="submit" class="btn btn-dark">Filter</button>
            </form>
        </div>
        {{/if}}
        <div class="col-md-12">
            <div class="card mt-3 bg-dark text-light">
                <div class="card-body">
                    <h5 class="card-title">Customers list</h5>
                    <table class="table text-light">
                        <tr>
                            <th>Photo</th>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Surname</th>
                            <th>Phone</th>
                            <th>Email</th>
                            <th>Company</th>
                        </tr>
                        {{#each customers}}
                        <tr>
                            <td>
                                {{#if photo}}
                                <img width="50" src="/uploads/{{photo}}">
                                {{/if}}
                            </td>
                            <td>{{id}}</td>
                            <td>{{name}}</td>
                            <td>{{surname}}</td>
                            <td>{{phone}}</td>
                            <td>{{email}}</td>
                            <td>{{#if company_name}}{{company_name}}{{else}}Unemployed{{/if}}</td>
                            <td>
                                <a href="/edit-customer/{{id}}" class="btn btn-success">Edit</a>
                                <a href="/delete-customer/{{id}}" class="btn btn-danger">Delete</a>
                                
                            </td>

                        </tr>
                        {{/each}}

                    </table>
                </div>
            </div>
        </div>
            {{>pagination}}
        </div>
    </div>
</main>
